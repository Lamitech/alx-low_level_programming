#include <stdio.h>
/**
 * main - starting point of all c program.
 * Return: return 0 when function is successful.
 */ 
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
